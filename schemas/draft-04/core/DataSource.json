{
  "id": "core/DataSource.json",
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Data source",
  "type": "object",
  "description": "Globaly unique identification of a data source.\n\nThe catalog of valid data sources is owned by AAM. Some identifiers are\nreserved and have been assigned to the Solutions or to a thirdparty\n(Twitter, iOS IDFS, People Device Graph, etc...) but new identifiers can be\ncreated on demand by AAM to represent customer’s data sources.\n\nSee also [AAM Data Source API](https://bank.demdex.com/portal/swagger/index.html#!/Data_Source_API/).\n",
  "properties": {
    "id": {
      "type": "integer",
      "description": "Globaly unique identifier of the data source.\n"
    },
    "tags": {
      "type": "array",
      "description": "Tags are used to indicate how the aliases represented by a given data\nsource should be interpreted by applications using those aliases.\n\nExamples:\n\n* `isAVID`: data sources representing Analytics visitor IDs.\n* `isCRSKey`: data sources representing aliases that should be used as keys in CRS.\n\nTags are set when the data source is created but they are also included in\npipeline messages when referencing a given data source.\n",
      "uniqueItems": true,
      "items": {
        "type": "string"
      }
    }
  },
  "required": [
    "id"
  ]
}