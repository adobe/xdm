Environment, device, and place-context dimensions for aggregated web-request summary records.
Reuses the standard Place Context, Device, and Environment datatypes so the field shapes match those emitted on interactive ExperienceEvents (for example by the Edge orchestration layer), giving analysts a consistent geo/device/environment model across interactive and summary datasets.
