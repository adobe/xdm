{
  "id": "core/Address.json",
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Address",
  "type": "object",
  "description": "A postal address. Address could relate to a persons home, work, preferred store location etc.",
  "properties": {
    "primary": {
      "type": "boolean",
      "description": "Primary address indicator."
    },
    "type": {
      "type": "string",
      "description": "The way the address relates to the person. e.g 'work' or 'residential'. Note this is person neutral, all persons will have a common address.type, if it is person specific the relationship attribute should be used e.g address.relationship='residence'",
      "enum": [
        "home",
        "work",
        "unknown"
      ]
    },
    "label": {
      "type": "string",
      "description": "Free form name of the address."
    },
    "street1": {
      "type": "string",
      "description": "Primary Street level information, apartment number, street number and street name."
    },
    "street2": {
      "type": "string",
      "description": "Optional street information second line."
    },
    "street3": {
      "type": "string",
      "description": "Optional street information third line."
    },
    "city": {
      "type": "string",
      "description": "The town, city, village or other metropolitan identity of the address."
    },
    "region": {
      "type": "string",
      "description": "The region, county, or district portion of the address."
    },
    "stateProvince": {
      "type": "string",
      "description": "The state,province,region,territory portion of the address."
    },
    "postalCode": {
      "type": "string",
      "description": "The postal code, zip code of other postal ordering for the address. Note, if zip codes are used either the base zip or zip+4 format can be used."
    },
    "country": {
      "type": "string",
      "description": "The name of the government-administered territory."
    },
    "latitude": {
      "type": "number",
      "description": "Defines the latitude of the delivery address.",
      "format": "float"
    },
    "longitude": {
      "type": "number",
      "description": "Defines the longtitude of the delivery address.",
      "format": "float"
    },
    "status": {
      "type": "string",
      "description": "An indication as to the ability to use the address.",
      "enum": [
        "active",
        "pendingVerification",
        "blacklisted",
        "blocked"
      ]
    },
    "statusReason": {
      "type": "string",
      "description": "A description of the current status."
    },
    "validity": {
      "type": "string",
      "description": "A level of technical correctness of the address.",
      "enum": [
        "consistent",
        "inconsistent",
        "incomplete",
        "successfullyUsed"
      ]
    },
    "lastVerifiedDate": {
      "type": "string",
      "format": "date-only",
      "description": "The date that the address was last verified as still belonging to the person. "
    },
    "audit": {
      "$ref": "core/AuditTrail.json"
    }
  }
}