Derived classification dimensions for web-request summary records that have no standard datatype home.
Currently carries a normalized device type derived from the raw hardware type and the bot detection signal, complementing the raw device type value.
